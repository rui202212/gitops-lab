apiVersion: v1
kind: Service
metadata:
  name: concrete-service
spec:
  type: NodePort
  selector:
    app: concrete-calculator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080  
